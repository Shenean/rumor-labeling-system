基于您提供的 `file\Backend` 内容（对应首轮对话中的系统实现报告），我将完整实现后端业务逻辑。由于报告中仅提供了设计思路而无具体代码，我将依据设计要求编写符合 Flask 最佳实践的代码。

## 实现计划

### 1. 基础组件与工具 (`app/utils/`)
*   **模拟模型 (`model_mock.py`)**：实现一个模拟的 NLP 模型类，用于模拟谣言检测和置信度评分（因无真实模型文件）。
*   **鉴权工具 (`auth.py`)**：实现基于 JWT 的 Token 生成与验证装饰器。

### 2. 核心业务路由实现 (`app/routes/`)
*   **用户模块 (`user.py`)**：
    *   实现注册 (`/register`) 与登录 (`/login`)。
    *   集成密码哈希 (`werkzeug.security`) 与 Token 签发。
*   **谣言检测模块 (`rumor.py`)**：
    *   实现 `/detect` 接口：接收文本 -> 调用模拟模型 -> 存入 `samples` 和 `model_logs` -> 返回结果。
*   **样本管理模块 (`sample.py`)**：
    *   实现样本的增删改查 (CRUD) 接口，支持分页与搜索。
*   **标注模块 (`annotate.py`)**：
    *   实现任务获取 (`/tasks`)：分配未标注样本。
    *   实现标注提交 (`/submit`)：写入 `annotations` 表并更新样本状态。
*   **事件聚合模块 (`event.py`)**：
    *   实现 `/cluster`：模拟聚类算法，将相似样本归为同一事件。
    *   实现事件列表查询。
*   **导出模块 (`export.py`)**：
    *   实现 `/data`：将标注数据导出为 CSV/Excel 文件（需添加 `pandas` 依赖）。

### 3. 配置与依赖更新
*   更新 `requirements.txt`：添加 `PyJWT`, `pandas`, `openpyxl`。
*   配置跨域支持 (CORS)：确保前端能正常调用接口。

请确认是否开始执行此后端实现计划？